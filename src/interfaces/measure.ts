export interface IDimension {
  width: number;
  height: number;
}

export interface IPosition {
  row: number;
  column: number;
}

export interface ITextLayerOffset {
  top: number;
  left: number;
}
